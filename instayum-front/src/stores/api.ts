/**
 * Store for API related values
 */
export const api = {
  // Backend was hardcoded instead of using environment variable (for debugging purposes)
  // Normally this would have a reference to an environment variable stored in the cloud
  API_PATH: "https://backend-dot-instayum.uw.r.appspot.com",
}
